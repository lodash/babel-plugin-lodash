import { map as map1 } from 'lodash';
import { map as map2 } from 'lodash';
import _ from 'lodash';
import l from 'lodash';

const result = map1([1, 2, 3], function() {});
const result2 = map2([], function() {});
const result3 = _.map([], function() {});
const result4 = l.map([], function() {});
