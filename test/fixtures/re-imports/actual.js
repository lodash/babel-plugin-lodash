import {map as map1} from 'lodash';
import {map as map2} from 'lodash';
import _ from 'lodash';
import l from 'lodash';

let result = map1([1, 2, 3], function() {});
let result2 = map2([], function() {});
let result3 = _.map([], function() {});
let result4 = l.map([], function() {});
